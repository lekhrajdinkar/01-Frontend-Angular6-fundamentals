import { Dropdown1Directive } from './dropdown1.directive';

describe('Dropdown1Directive', () => {
  it('should create an instance', () => {
    const directive = new Dropdown1Directive();
    expect(directive).toBeTruthy();
  });
});
