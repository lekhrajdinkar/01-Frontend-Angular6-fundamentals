import { Basic1Directive } from './basic-1.directive';

describe('Basic1Directive', () => {
  it('should create an instance', () => {
    const directive = new Basic1Directive();
    expect(directive).toBeTruthy();
  });
});
